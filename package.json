{
  "name": "sherquote-mock",
  "version": "1.0.0",
  "description": "SherQuote mock API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 8080",
    "server": "json-server --watch db.json"
  },
  "author": "Martin Rancourt",
  "license": "MIT",
  "devDependencies": {
    "json-server": "^0.9.4"
  }
}
